RSpec.describe Simplecov::Stdout::Line::Printer do
  it "has a version number" do
    expect(Simplecov::Stdout::Line::Printer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
